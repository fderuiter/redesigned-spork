#' Mednet EDC API
#'
#' The Mednet EDC (Electronic Data Capture) REST API provides a single-point of access for reading data stored across iMednet data services. This specification documents all available endpoints in the latest version of the API. 
#'
#' The version of the OpenAPI document: 1.0.15
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Jobs operations
#' @description JobsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetJobStatus  ####################
#'
#' library(openapi)
#' var_study_key <- "study_key_example" # character | Study key identifying the study context for the request
#' var_batch_id <- "batch_id_example" # character | Batch ID of the job to retrieve
#'
#' #Retrieve job status by batch ID
#' api_instance <- JobsApi$new()
#'
#' # Configure API key authorization: apiKeyAuth
#' api_instance$api_client$api_keys["x-api-key"] <- Sys.getenv("API_KEY")
#'
#' # Configure API key authorization: securityKeyAuth
#' api_instance$api_client$api_keys["x-imn-security-key"] <- Sys.getenv("API_KEY")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJobStatus(var_study_key, var_batch_iddata_file = "result.txt")
#' result <- api_instance$GetJobStatus(var_study_key, var_batch_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
JobsApi <- R6::R6Class(
  "JobsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new JobsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Retrieve job status by batch ID
    #'
    #' @param study_key Study key identifying the study context for the request
    #' @param batch_id Batch ID of the job to retrieve
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Job
    GetJobStatus = function(study_key, batch_id, data_file = NULL, ...) {
      local_var_response <- self$GetJobStatusWithHttpInfo(study_key, batch_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve job status by batch ID
    #'
    #' @param study_key Study key identifying the study context for the request
    #' @param batch_id Batch ID of the job to retrieve
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Job) with additional information such as HTTP status code, headers
    GetJobStatusWithHttpInfo = function(study_key, batch_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`study_key`)) {
        stop("Missing required parameter `study_key`.")
      }

      if (missing(`batch_id`)) {
        stop("Missing required parameter `batch_id`.")
      }



      local_var_url_path <- "/studies/{studyKey}/jobs/{batchId}"
      if (!missing(`study_key`)) {
        local_var_url_path <- gsub("\\{studyKey\\}", URLencode(as.character(`study_key`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`batch_id`)) {
        local_var_url_path <- gsub("\\{batchId\\}", URLencode(as.character(`batch_id`), reserved = TRUE), local_var_url_path)
      }

      # API key authentication
      if ("x-api-key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["x-api-key"]) > 0) {
        header_params["x-api-key"] <- paste(unlist(self$api_client$api_keys["x-api-key"]), collapse = "")
      }
      # API key authentication
      if ("x-imn-security-key" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["x-imn-security-key"]) > 0) {
        header_params["x-imn-security-key"] <- paste(unlist(self$api_client$api_keys["x-imn-security-key"]), collapse = "")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "Job"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
